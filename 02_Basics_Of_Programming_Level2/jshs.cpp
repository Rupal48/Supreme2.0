#include <iostream>
using namespace std;
// bool some(unsigned int x) {
//     return (!(x&(x-1)));
// }
int main() {
    // cout << some(0);
    // int num = 8;
    // cout << (~num + 1);
    // if (~0 == 1) cout <<"yes";
    // else cout << "no";
    int x = -9;
    x = x >> 1;
    cout << x;
    return 0;
}